apiVersion: v1
kind: Service
metadata:
  name: {{ include "obol-app.fullname" . }}
  labels:
    {{- include "obol-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "obol-app.selectorLabels" . | nindent 4 }}
