{{- if not .Values.charon.externalServices.consensusEndpoint }}
{{- fail ".Values.charon.externalServices.consensusEndpoint is required" }}
{{- end }}


{{- if not .Values.secrets.enrPrivateKey }}
{{- fail ".Values.secrets.enrPrivateKey is required" }}
{{- end }}

{{- if not .Values.secrets.clusterlock }}
{{- fail ".Values.secrets.clusterlock is required" }}
{{- end }}

{{- if and (not .Values.charon.operatorAddress) (not .Values.tests) }}
{{- fail ".Values.charon.operatorAddress is required" }}
{{- end }}

{{- if .Values.centralMonitoring.enabled }}
{{- if not .Values.centralMonitoring.token }}
{{- fail ".Values.centralMonitoring.token is required" }}
{{- end }}
{{- end }}
